{
  "name": "ai-thought-leadership-api",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless function + static site",
  "engines": { "node": "22.x" },
  "main": "generate-insight.js",
  "scripts": {
    "build": "echo \"no build step\"",
    "dev": "vercel dev",
    "start": "vercel dev",
    "test": "node --test ./test/*.test.js"
  },
  "dependencies": {}
}
